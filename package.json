{
  "private": true,
  "type": "module",
  "name": "infoblend",
  "version": "3.0.0",
  "description": "Chrome extension for instant definitions and page summaries.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
